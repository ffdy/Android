<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
        xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:tools="http://schemas.android.com/tools"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        tools:context=".MainActivity">

    <fragment
            android:id="@+id/title"
            app:layout_constraintTop_toTopOf="parent"
            android:layout_width="match_parent"
            android:name="cn.edu.swu.h4.TitleFragment"
            tools:layout="@layout/fragment_title"
            android:layout_height="wrap_content"/>

    <TextView
            android:id="@+id/subtitle_todo"
            android:text="Todo"
            android:textColor="@color/black"
            android:textStyle="bold"
            android:textSize="28sp"
            android:padding="10dp"
            app:layout_constraintTop_toBottomOf="@id/title"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"/>

    <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/recyclerView_todo"
            android:padding="10dp"
            app:layout_constraintTop_toBottomOf="@+id/subtitle_todo"
            tools:listitem="@layout/item_todo"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"/>

    <TextView
            android:id="@+id/subtitle_done"
            android:text="Done"
            android:textColor="@color/black"
            android:textStyle="bold"
            android:textSize="28sp"
            android:padding="10dp"
            app:layout_constraintTop_toBottomOf="@id/recyclerView_todo"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"/>

    <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/recyclerView_done"
            android:padding="10dp"
            app:layout_constraintTop_toBottomOf="@id/subtitle_done"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"/>

</androidx.constraintlayout.widget.ConstraintLayout>